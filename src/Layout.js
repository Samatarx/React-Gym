import React from 'react'
import Nav from './Nav'
import Footer from './Footer'

function Layout(children) {
    return (
        <main>
            <Nav/>
            
            <Footer/>
        </main>
)
}

export default Layout
